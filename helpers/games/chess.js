const chessJS = require("chess.js")
const { aiMove } = require("js-chess-engine")

// Some day I hope to move to a custom Chess engine

class Chess {
    constructor(data){
        if (data){

        }
    }
    ai(){
        return aiMove(FFN, level)
    }
}

module.exports = Chess

