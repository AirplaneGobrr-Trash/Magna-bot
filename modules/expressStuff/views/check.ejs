<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hi</title>
    <script src="http://magna.airplanegobrr.xyz/socket.io/socket.io.js"></script>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
</head>
<body>
    <h1>Hello world!</h1>
    <select id="picker"></select>
    <div id="editor"></div>
    <textarea id="jsonEditor"></textarea>
    <button id="update">Update</button>
    <script>
        var socket = io("http://magna.airplanegobrr.xyz");

        const editor = document.getElementById("editor")
        const picker = document.getElementById("picker")
        const jsonEditor = document.getElementById("jsonEditor")
        const update = document.getElementById("update")

        async function updateUser(userID, userData){

        }

        async function updateServer(serverID, serverData){

        }

        var setup = false
        socket.once("checked", (data)=>{
            setup = true
            console.log("once\n",data)
            var opt = document.createElement("option")
            opt.innerText = "-- Servers --"
            picker.append(opt)

            for (var server in data.data.allServers){
                var serverData = data.data.allServers[server]
                var opt = document.createElement("option")
                opt.innerText = `${serverData?.name} (${server})`
                opt.type = "server"
                opt.id = server
                picker.append(opt)
            }

            var opt = document.createElement("option")
            opt.innerText = "-- Users --"
            picker.append(opt)
            for (var user in data.data.allUsers){
                var userData = data.data.allUsers[user]
                var opt = document.createElement("option")
                opt.innerText = `${userData?.name} (${user})`
                opt.type = "user"
                opt.id = user
                picker.append(opt)
            }

            var currentPick = {}

            picker.onchange = (e) => {
                console.log(e)
                var currentOpt = e.target.options[e.target.options.selectedIndex]
                currentPick.elm = currentOpt

                if (currentOpt.type == "server") {
                    console.log("Server", currentOpt.id)
                    var serverData = data.data.allServers[currentOpt.id]
                    currentPick.data = serverData
                    currentPick.type = "server"
                    console.log(serverData)
                    jsonEditor.innerText = JSON.stringify(serverData)

                } else if (currentOpt.type == "user") {
                    console.log("User!", currentOpt.id)
                    var userData = data.data.allUsers[currentOpt.id]
                    currentPick.data = userData
                    currentPick.type = "user"
                    console.log(userData)
                    jsonEditor.innerText = JSON.stringify(userData)

                }
            }

            update.onclick = (e) => {
                console.log(e,currentPick)
            }
        })

        socket.on("checked", (data)=>{
            if (!setup) return
            console.log(data)
        })

        socket.emit("getCheck")

      </script>
</body>
</html>